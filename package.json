{
  "name": "mini-militia-evvm",
  "version": "1.0.0",
  "description": "Mini Militia game with EVVM staking integration",
  "type": "module",
  "scripts": {
    "dev": "vite mini-militia-fresh",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && vercel --prod"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {}
}
