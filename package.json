{
  "name": "stakearena",
  "version": "1.0.0",
  "description": "StakeArena - Survival game with EVVM staking integration",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node build.js",
    "preview": "vite preview",
    "deploy": "npm run build && vercel --prod"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {}
}
